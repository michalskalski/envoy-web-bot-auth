envoy-web-bot-auth-module

This archive contains the Envoy dynamic module for the recorded Envoy runtime
line. Load libenvoy_web_bot_auth.so through Envoy's dynamic module extension.

The supported runtime, SDK revision, protocol profile, and build target are in
compatibility.json. The module is built for the architecture in the filename.
