[package]
name = "web-bot-auth-protocol"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Versioned Web Bot Auth resolver wire types and discovery rules"
publish = false

[dependencies]
base64 = "0.22"
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.11"
url = "2.5"

[dev-dependencies]
serde_json = "1.0"
